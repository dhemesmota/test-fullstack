/* eslint-disable react/prop-types */
import React from 'react';

import { Container } from './styles';

const Table: React.FC = ({ children }) => <Container>{children}</Container>;

export default Table;
